/**
 * Application UI fonts.
 *
 * @export
 * @enum {number}
 */
export enum Fonts {
    Primary = 'Arial, Helvetica, sans-serif',
    Secondary = 'Arial, Helvetica, sans-serif',
}

/**
 * Application UI font sizes.
 *
 * @export
 * @enum {number}
 */
export enum FontSizes {
    Large = '2rem',
    Medium = '1.5rem',
}
